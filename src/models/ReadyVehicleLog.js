class ReadyVehicleLogModel {
  constructor(id, regNo, deliveredOn, totalPrize) {
    this.id = id;
    this.regNo = regNo;
    this.deliveredOn = deliveredOn;
    this.totalPrize = totalPrize;
  }
}

export default ReadyVehicleLogModel;
